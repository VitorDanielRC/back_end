services:
  db:
    image: postgres:15
    environment:
      - POSTGRES_USER: dev
      - POSTGRES_PASSWORD: dev123
      - POSTGRES_DB: loja
    ports:
      - "5432 : 5432"

    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: